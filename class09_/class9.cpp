/*数学函数
#include <cmath>

1.pow (a,b)
乘方，用于计算a的b次方
2.sqrt(a)
用于开方
3。abs (a)
用于算绝对值
4.fmod (a,b)
浮点数取余数
5.ceil (a)
向上取整
6.floor (a)
向下取整
7.hypot(a,b)
用勾股定理算斜边
8.sin,cos,
9.log
默认以10为底，所以只有一个参数，想要任意底数的需要自己算，log(a)/log(b)
*/